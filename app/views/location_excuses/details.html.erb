<div id="pagepiling">
  <div class="section">
    <div class="details-page" style="align-items: space-between; justify-content: center;">
      <% @location_excuse.lines_disrupted.to_i > 0 ? @mode = "bus" : @mode = "line" %>
        <% @mode = "road" if @location_excuse.journeys  == [] %>
        <% if @mode == "road" %>
        <h2 class="mt-3 text-center">There are traffic delays <%= @location_excuse.lines_disrupted %></h2>
        <% else %>
        <h2 class="mt-3 text-center"><%= @location_excuse.lines_disrupted %> <%= @mode %></h2>
        <h2 style="font-size: 21px;"> has disruptions</h2>
        <% end %>
        <% @coordinates = [] %>
        <% @polyline = [] %>
            <% @location_excuse.journey_route&.each do |leg|%>
              <% if @location_excuse.lines_disrupted == 'Driving' %>
                <% @polyline << leg %>
              <% else %>
              <% unless leg.class == Array %>
                <% leg = JSON.parse(leg) %>
                <% leg.each do |coords| %>
                  <% @coordinates << [coords[1], coords[0]] %>
                <% end %>

              <% else %>

                <% @location_excuse.journey_route&.each do |coords| %>
                  <% @coordinates << [coords[1].to_f, coords[0].to_f] %>
                <% end %>

              <% end %>
              <% end %>
            <% end %>

            <div id="coordinates" style="display: none;" >
              <%= @coordinates %>
            </div>
            <div id="polyline" style="display: none;" >
              <%= @polyline %>
            </div>
      <div
        id="map"
        style="width: 100%;
        height: 300px;"
        data-markers="<%= @markers.to_json %>"
      ></div>

      <p><span id="traffic" class="link">Traffic Overlay</span><span id="transit" class="link"> - Transit Overlay</span></p>
        <div class="details-page"  style="align-items: center; justify-content: center;">
          <div class="mt-3 width-90">
            <p><%= @location_excuse.disruption_message %></p>
          </div>
        </div>
      </div>
    </div>
  <div class="section sec-2">
    <div class="details-page width-90" style="align-items: center; justify-content: center;">
      <div class="width-90">
        <p style="color:#FFBA34;">EXCUSE MESSAGE</p>
        <div class ="p-3 d-flex justify-content-between" style="color:#575757; background-color:#F4F4F4;" contenteditable="true">
          I’m sorry, I’m running late this morning.
          The <%= @location_excuse.lines_disrupted %> <%= @mode %> is disrupted and
          has affected my route. It will take me <%= @location_excuse.journeys[1] if @location_excuse.journeys %> minutes to get in.
          <div style="background-color: #9CF0F5; padding: 6px 8px; padding-bottom: 2px; border-radius: 50%; height: 34px; width: 34px; object-fit: cover;" contenteditable="false">
            <i class="far fa-copy" style="font-size: 23px; color: white;"></i>
          </div>
        </div>
        <%= link_to "🔙", location_excuse_path(@location_excuse), class: "link m-3 big" %>
      </div>
    </div>
  </div>
  <% unless @location_excuse.journeys&.empty? %>
  <div class="section sec-3">
    <div class="details-page width-90" style="align-items: center; justify-content: center;">
      <div class="width-90 mt-3">
        <p style="color:#FFBA34;">DISRUPTED JOURNEY<span style="color:#575757"> - <%= @location_excuse.journeys[1] if @location_excuse.journeys%> minutes</span></p>
          <ul>
          <% @location_excuse.journeys&.drop(2)&.each do  |journey|%>
            <li style="color:#575757"><%= journey %></li>
          <% end %>
          </ul>
      </div>
      <%= link_to "🔙", location_excuse_path(@location_excuse), class: "link m-3 big" %>
    </div>
  </div>
  <% end %>
</div>
